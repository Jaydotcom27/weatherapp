import React from 'react'

const Results = ({ infos }) => {
  return (
    <div className="search">
      {!infos.length ? <h1>Info not Found</h1> : infos.map(info) => {
          return
          <Weather
            
      }}
    </div>
  )
}
